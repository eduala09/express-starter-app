Git Life Cycle:

git init => To initialize the environment for Git

git add . => To stage

git status

git commit -m "first commit" => To commit

git status

git branch -M main => To specify the branch where the code will reside

git remote add origin https://github.com/eduala09/express-starter-app.git 

git remote -v

git push -u origin main